import Navigation from "./Navigation";
import Logo from "./Logo"

const Header = () => {
    return (
        <div id="header">
            <Logo />
            <Navigation />
        </div>
    );
}

export default Header;